let height = 10;
let base = 4;
let recPerimeter = (height + base) * 2
let recArea = height * base 
console.log(`The perimeter of a rectangle with height of ${height} and base of ${base} is ${recPerimeter}`);
console.log(`The area of a rectangle with height of ${height} and base of ${base} is ${recArea}`);